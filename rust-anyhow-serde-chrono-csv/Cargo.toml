[package]
name = "rust-anyhow-serde-chrono-csv"
version = "0.1.0"
edition = "2021"
description = "Rust 基础工程化工具链示例：anyhow、serde、chrono、csv"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }
csv = "1"
serde_json = "1"
